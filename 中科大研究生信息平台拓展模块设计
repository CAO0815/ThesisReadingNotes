一、总体架构

1、C/S和B/S的主要区别

基于C/S架构一般用于安全性要求非常高、应用较为复杂的情况；而且这种模式的开发、维护成本较高；对于开发者，客户端的开发需要考虑各种因素，比如操作系统的不同，设备性能的不同等等。

B/S架构是运行在web浏览器中的程序，运行环境较为单一，所以开发起来较为便捷。无需安装客户端，在浏览器中发送请求就可获取自己想要的内容，如要更新内容，直接在服务器端更新即可，
能更快而且同步让所有人看到最新的内容。但这种模式要花费较多的精力去处理安全问题，要防止各种窃听、网络攻击等。
